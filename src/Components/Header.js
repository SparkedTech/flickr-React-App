import React from 'react';

//Component Imports
import Nav from './Nav';

const Header = (props) => {
    return (
        <div>
            <h1>Flickr Image Search App</h1>
            <Nav />
        </div>
    )
};

export default Header;